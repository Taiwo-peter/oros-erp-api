spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: test_user
    password: test_pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: never